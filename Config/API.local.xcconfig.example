// Copy to API.local.xcconfig (gitignored) to point Release builds at your
// own backend. Debug builds already default to localhost and need nothing
// here.
//
// Write the host without a scheme and without "//" — an xcconfig treats "//"
// as the start of a comment.

LOCAL_API_SCHEME = https
LOCAL_API_HOST = your-tunnel-hostname.example.com
